<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Wallet</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .wallet-container {
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            width: 100%;
            max-width: 400px;
        }
        .wallet-header {
            background-color: #088178;
            color: white;
            padding: 20px;
            text-align: center;
        }
        .wallet-header h1 {
            margin: 0;
            font-size: 24px;
        }
        .balance-overview {
            background-color: #f9f9f9;
            padding: 20px;
            text-align: center;
        }
        .balance-overview h2 {
            margin: 0 0 10px;
            color: #333;
        }
        .balance {
            font-size: 36px;
            font-weight: bold;
            color: #088178;
        }
        .transaction-history {
            padding: 20px;
        }
        .transaction-history h2 {
            margin: 0 0 15px;
            color: #333;
        }
        .transaction {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }
        .transaction:last-child {
            border-bottom: none;
        }
        .transaction-date {
            color: #888;
            font-size: 14px;
        }
        .transaction-amount {
            font-weight: bold;
        }
        .transaction-amount.positive {
            color: #088178;
        }
        .transaction-amount.negative {
            color: #e74c3c;
        }
        .add-money {
            padding: 20px;
            text-align: center;
        }
        .add-money-btn {
            background-color: #088178;
            color: white;
            border: none;
            padding: 12px 24px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .add-money-btn:hover {
            background-color: #066660;
        }
    </style>
</head>
<body>
    <div class="wallet-container">
        <div class="wallet-header">
            <h1>My Wallet</h1>
        </div>
        <div class="balance-overview">
            <h2>Balance Overview</h2>
            <div class="balance">Rs.<%= balance.toFixed(2) %></div> <!-- Displaying the balance dynamically -->
        </div>
        <div class="transaction-history">
            <h2>Transaction History</h2>
            <% if (transactions.length > 0) { %>
                <% transactions.forEach(transaction => { %>
                    <div class="transaction">
                        <div>
                            <div><%= transaction.description %></div>
                            <div class="transaction-date"><%= transaction.date.toLocaleDateString() %></div> <!-- Assuming you stored the transaction date -->
                        </div>
                        <div class="transaction-amount <%= transaction.type === 'credit' ? 'positive' : 'negative' %>">
                            <%= transaction.type === 'credit' ? '+' : '-' %> Rs.<%= transaction.amount.toFixed(2) %>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No transactions available</p>
            <% } %>
        </div>
        <div class="add-money">
            <button class="add-money-btn">Add Money</button>
        </div>
    </div>
</body>

</html>